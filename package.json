{
  "name": "shootingiq",
  "version": "1.0.0",
  "private": true,
  "description": "ShootingIQ — a single-file PWA for tracking solo basketball shooting workouts.",
  "scripts": {
    "test": "node tests/run.mjs",
    "postinstall-note": "npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
